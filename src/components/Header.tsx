import { Fuel } from 'lucide-react';
import '../styles/Header.scss'

const Header = () => {
  return (
    <header>
        <Fuel />
        <h1>Carbu map</h1>
    </header>
  )
}

export default Header